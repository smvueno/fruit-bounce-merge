# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Base path for deployment (optional)
# Examples:
#   VITE_BASE_PATH=/                       (root deployment - default)
#   VITE_BASE_PATH=/fruit-bounce-merge/    (GitHub Pages main deployment)
#   VITE_BASE_PATH=/test/                  (GitHub Pages test branch)
# 
# Leave empty or set to / for root deployment
# During development, the base is always / regardless of this setting
VITE_BASE_PATH=/
